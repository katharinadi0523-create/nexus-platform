"use client";

import { SkillsPage } from "@/components/skills/skills-page";

/** Platform skills route — keep free of custom props for Next.js PageProps. */
export default function SkillsRoutePage() {
  return <SkillsPage />;
}
